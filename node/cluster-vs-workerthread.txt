Cluster is used to scale Node.js applications across multiple CPU cores by creating separate processes, each with its own event loop and memory space. Worker threads, on the other hand, are used for parallel execution of CPU-intensive tasks within a single process using threads, allowing shared memory and faster communication. In real-world systems, cluster (or container scaling) handles high traffic, while worker threads handle heavy computation.